# =============================================================================
# Secure AI Pipeline for Sensitive Data — 2024DA04378
# Dependency manifest for the full dissertation (Sprints 1–6).
# =============================================================================
# Version pins are chosen for CPU-only Python 3.10–3.13 environments and for
# mutual compatibility (notably torch <-> opacus and fastapi <-> pydantic v2).
# =============================================================================

# ---- Mid-sem stack (already in use) ----------------------------------------
pandas>=2.2,<3.0
numpy>=1.26,<2.3
scikit-learn>=1.5,<1.8
cryptography>=43.0
torch>=2.2,<2.5
requests>=2.31

# ---- Sprint 1: persistence + parquet + serialisation -----------------------
joblib>=1.3
pyarrow>=15.0        # pandas parquet backend

# ---- Sprint 2: Differential Privacy (Configs 3 & 4) ------------------------
# Opacus 1.5.x is the last line compatible with torch 2.2–2.4.
opacus>=1.5,<1.6

# ---- Sprint 3: FastAPI inference service + JWT + RBAC ----------------------
fastapi>=0.115,<0.120
uvicorn[standard]>=0.30,<0.35
pydantic>=2.7,<3.0
python-jose[cryptography]>=3.3,<4.0   # JWT encode/decode
passlib[bcrypt]>=1.7,<2.0             # password hashing
python-multipart>=0.0.9               # OAuth2PasswordRequestForm needs this
httpx>=0.27,<0.29                     # TestClient + in-process integration tests

# ---- Sprint 3: Chained-hash audit log --------------------------------------
sqlalchemy>=2.0,<3.0

# ---- Sprint 5: Pareto plot + attack diagnostics ----------------------------
matplotlib>=3.8,<4.0
seaborn>=0.13,<0.15
